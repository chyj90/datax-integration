spring:
  application:
      name: server-datax
  profiles:
    active: dev
eureka:
  client:
    enabled: true
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always